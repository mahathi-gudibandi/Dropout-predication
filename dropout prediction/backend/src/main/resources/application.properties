# Server
server.port=8081

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/dropout_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Secret (change this in production)
jwt.secret=StudentDropoutPredictionSecretKey2024VeryLongSecureKey
jwt.expiration=86400000

# ML Service URL
ml.service.url=http://localhost:5000/predict

# CORS
cors.allowed.origins=http://localhost:3000,http://127.0.0.1:5500,http://localhost:8081

# Email (Gmail SMTP) — replace with your Gmail credentials
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=srivatsav.y245@gmail.com
spring.mail.password=azcasrukwemwksvo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.enabled=true
app.mail.from=srivatsav.y245@gmail.com
